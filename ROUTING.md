# 国内平台路由策略

本项目只调度已完成端到端验证的国内平台：`qwen`、`doubao`、`kimi`、`minimaxi`、`longcat`。

## 选择规则

| 问题类型 | 首选 | 备选 | 说明 |
|---|---|---|---|
| 中文公开网页、阿里相关、通用调研 | Qwen | Kimi | Qwen 的网页检索和完成信号提取已稳定验证 |
| 抖音、头条、中文热点与内容归纳 | 豆包 | Qwen | 豆包对字节内容语境更合适；注意可能出现平台限流 |
| 长文档、研究型问题、多来源整理 | Kimi | Qwen | Kimi 会暴露 Think 块，执行器会只取最终 Markdown 正文 |
| MiniMax Agent、通用中文问答 | MiniMax | Kimi | 使用 MiniMax Agent 的 ProseMirror 编辑器路径 |
| 本地生活、美团语境、联网中文检索 | LongCat | Qwen | LongCat 可显示检索状态；执行器只回收最终 Markdown 回答 |

## 单平台优先

下列情况不要批量：

- 用户明确指定某个平台；
- 问题明显属于某一数据生态；
- 平台限额紧张，或只需要一个可验证答案；
- 需要快速完成，且没有交叉验证需求。

## 批量策略

适合多平台批量的情况：

- 时效性强、需要交叉验证的发布、价格、模型能力或新闻问题；
- 结论风险高，需要比较不同平台证据；
- 用户明确要求多平台搜索。

批量执行器会：

1. 顺序打开 tab、加载 session、提交问题；
2. 并发轮询各平台的最终回答；
3. 对短暂的 camofox RPC 超时保留 tab 并重试；
4. 将每个平台结果写入独立文本文件，并在 `batch-summary.json` 标明失败原因。

## 证据标准

无论使用哪个平台：

1. 官方公告、官方文档、官方 API 页面优先；
2. 独立测试与媒体报道必须显式标注来源类别；
3. 搜索结果、模型“思考过程”、UI 状态和无来源断言都不是最终事实；
4. 同一说法未找到一手来源时，标为“待验证”，不要写成正式发布事实。

## 不调度的平台

本地维护策略明确排除国外平台，不在 CLI 或批量执行器中提供：Gemini、Grok 等。

元宝目前未通过可复用登录态与长回答验证，也不在默认可调度列表中。
